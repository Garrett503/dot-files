#!/bin/sh                                    
                                             
# Source the colors from wal                 
source "${HOME}/.cache/wal/colors.sh"        
                                             
ln -sf    "${HOME}/.cache/wal/cava.config" >                            
